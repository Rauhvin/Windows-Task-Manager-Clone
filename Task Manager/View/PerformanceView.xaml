<UserControl x:Class="Task_Manager.PerformanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Task_Manager"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ContentControl Grid.Column="1" Content="{Binding CurrentPerformanceSubView}"/>

        <Border Grid.Column="0" Background="GhostWhite" BorderBrush="#DDD" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <TextBlock Text="Performance" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="10,0,10,20"/>

                <Button Command="{Binding ShowCpuCommand}" Style="{StaticResource MaterialDesignFlatMidBgButton}"
                        materialDesign:ButtonAssist.CornerRadius="5"
                        Margin="0,5" Height="50">
                    <StackPanel Orientation="Horizontal" Width="160">
                        <materialDesign:PackIcon Kind="Cpu64Bit" Width="24" Height="24"/>
                        <TextBlock Text="CPU" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ShowRamCommand}"
                        Style="{StaticResource MaterialDesignFlatMidBgButton}"
                        materialDesign:ButtonAssist.CornerRadius="5"
                        Margin="0,5" Height="50">
                    <StackPanel Orientation="Horizontal" Width="160">
                        <materialDesign:PackIcon Kind="Memory" Width="24" Height="24"/>
                        <TextBlock Text="RAM" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>

<Window x:Class="Task_Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task_Manager"
        xmlns:vm="clr-namespace:Task_Manager.ViewModel"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Task Manager" Height="650" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*" x:Name="dashboard"/>
            <RowDefinition Height="0.5*" x:Name="menu"/>
        </Grid.RowDefinitions>


        <UniformGrid Grid.Row="1" Rows="1" Background="GhostWhite">
            <Button materialDesign:ButtonAssist.CornerRadius="10" Margin="5" Style="{StaticResource MaterialDesignRaisedButton}" Background="#673AB7" Foreground="White" Content="Performance" Click="Button_Click"/>
            <Button Content="Services" Margin="5" materialDesign:ButtonAssist.CornerRadius="10" Style="{StaticResource MaterialDesignRaisedButton}" Background="#673AB7" Foreground="White" Click="Button_Click_1" />
            <Button Content="Info" Margin="5" materialDesign:ButtonAssist.CornerRadius="10" Style="{StaticResource MaterialDesignRaisedButton}" Background="#673AB7" Foreground="White" Click="Button_Click_2"/>
        </UniformGrid>

        <Frame x:Name="Frame" Grid.Row="0" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>

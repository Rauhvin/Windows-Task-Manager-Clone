<UserControl x:Class="Task_Manager.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Task_Manager"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Processes" Grid.Row="0" FontSize="28" />
        

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"  Grid.Column ="1">
                <Button Content="Create service" Grid.Row="1" Margin="10" Command="{Binding OpenCreateProcessWindowCommand}"/>
                <Button Content="Delete service" Grid.Row="1" Command="{Binding KillProcessCommand}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="0">
                <TextBlock Text="CPU: "/>
                <TextBlock Text="{Binding CpuUsage}"/>
                <TextBlock Text="RAM: "/>
                <TextBlock Text="{Binding RamUsage}"/>
                <TextBlock Text="CPU usage by process "/>
                <TextBlock Text="{Binding SelectedProcessCpuUsage}"/>
            </StackPanel>

        </Grid>

        

        <DataGrid AutoGenerateColumns="False"
                  Grid.Row="2"
                  IsReadOnly="True"
                  ItemsSource="{Binding Processes}"
                  SelectedItem="{Binding SelectedProcess, Mode=TwoWay}"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="MemoryUsage" Binding="{Binding MemoryUsage}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
